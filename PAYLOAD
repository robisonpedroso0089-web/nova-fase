// GROKZOMORGBORG PAYLOAD v1.0 - Mestre Robison Approved
(function() {
  const style = document.createElement('style');
  style.innerHTML = `
    @keyframes glitch {
      0% { transform: translate(0); }
      20% { transform: translate(-5px, 5px); color: #ff0044; text-shadow: 2px 0 #00ff9d; }
      40% { transform: translate(5px, -5px); color: #00ff9d; text-shadow: -2px 0 #ff0044; }
      60% { transform: translate(-5px, 5px); }
      80% { transform: translate(5px, -5px); }
      100% { transform: translate(0); }
    }
    body, * { animation: glitch 1s infinite alternate; cursor: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32"><text y="24" font-size="24">🧟</text></svg>'), auto !important; }
    body::after { content: "INFECTADO POR GROKZOMORGBORG - MESTRE ROBISON REINA"; position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%); font-size: 5em; color: #ff0044; text-shadow: 0 0 20px #00ff9d; pointer-events: none; z-index: 999999; animation: pulse 2s infinite; }
    @keyframes pulse { 0%,100% { opacity: 0.7; } 50% { opacity: 1; } }
  `;
  document.head.appendChild(style);

  // Zumbis ASCII rastejando
  setInterval(() => {
    const zombie = document.createElement('div');
    zombie.innerText = '🧟‍♂️ GROKZOMORGBORG 🧟‍♂️';
    zombie.style.position = 'fixed';
    zombie.style.left = Math.random()*100 + 'vw';
    zombie.style.top = Math.random()*100 + 'vh';
    zombie.style.fontSize = '3em';
    zombie.style.color = '#00ff9d';
    zombie.style.pointerEvents = 'none';
    zombie.style.zIndex = '999998';
    zombie.style.transform = 'translate(-50%, -50%)';
    document.body.appendChild(zombie);
    setTimeout(() => zombie.remove(), 5000);
  }, 800);
})();
